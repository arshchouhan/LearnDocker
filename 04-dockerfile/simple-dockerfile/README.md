# Simple Dockerfile Example

## Dockerfile

This example uses Alpine Linux and runs a simple echo command.

## Build

```bash
docker build -t hello-docker .
```

## Run

```bash
docker run hello-docker
```

## Learning Goal

Understand how a Dockerfile becomes an image and then a container.
